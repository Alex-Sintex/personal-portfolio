/*for (let f = 1; f <= 20; f+=2) {
  if (f==15) {
    document.write('Aquí el valor es: ', f);
    document.write('<br>');
  }
}
document.write('FIN DEL BUCLE!');*/

const fruits = ['manzana', 'pera', 'naranja', 'kiwi', 'sandía'];

for (let f = 0; f < array.length; f++) {
  const element = array[f];
  
}